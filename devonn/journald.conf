# Journald configuration for DEVONN services
# Copy to /etc/systemd/journald.conf.d/devonn.conf

[Journal]
# Limit journal size per service
SystemMaxUse=500M
SystemMaxFileSize=50M
MaxFileSec=1week

# Compress older entries
Compress=yes

# Sync to disk every 5 seconds
SyncIntervalSec=5s
